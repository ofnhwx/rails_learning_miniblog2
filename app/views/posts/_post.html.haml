.row
  .col
    .card.mb-3
      %h5.card-header
        = link_to post.user.email, post.user
      .card-body
        = simple_format h(post.content)
      .card-footer
        .text-right= post.created_at
